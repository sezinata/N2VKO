Example dataset format, not an exact dataset

testindices start from 1 (R-code)

IntActSVMclassforalzheimer, file keeps all the labels for all nodes/proteins in the same order with train-test indices, starting from 1.

Data in KeywordsEmbeddingsUsefulSubset file obtained by performing a specific feature selection method before oversampling. Each feature selection has its own unique keywords set.

IntActMatrixforSVMvector file is keywords matrix for each protein, proteins are represented by rows.